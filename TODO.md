# TODO

작업 전 논의/우선순위 조율용 목록. 작성일 2026-06-10.

## 카드 UI/UX

- [ ] **1. 리사이즈 핸들 이미지 제거** — 현재 react-resizable 기본 핸들(우하단 화살표 아이콘 등)이 보임. 아이콘 없이 일반 윈도우 프로그램처럼 가장자리/모서리에 마우스 올리면 커서만 바뀌고 드래그로 리사이즈되게.
- [ ] **2. youtube/teams 웹뷰 카드의 SW/SE 리사이즈 안 됨** — child webview가 `.plugin-body` 전체를 덮어 핸들 클릭이 안 먹힘. 리사이즈 시작 시 webview를 임시로 hide/축소하는 연동 필요 (PluginCard ↔ 플러그인 간 "리사이즈 중" 상태 전달). [#1과 함께 다시 설계하면 좋음 — 보이지 않는 핸들이라면 영역 자체는 살아있어야 함]
- [ ] **3. 카드 z-index(노출 우선순위) 클릭으로 변경** — 클릭한 카드가 맨 앞으로 오도록. youtube/teams 웹뷰는 OS 레벨에서 항상 최상단이라 별도 처리(웹뷰 z-order는 #2 hide 연동과 연결될 수 있음) 필요.
- [ ] **11. 헤더(타이틀바) 호버 시에만 표시 옵션** — 카드 헤더(이름+✕)를 마우스 오버할 때만 보이게 하는 토글. 플러그인별 또는 전역 설정.
- [ ] **7. 플러그인 헤더 커스터마이즈** — 현재는 아이콘+이름+✕ 고정. 플러그인이 헤더에 자체 버튼(설정 등) 추가 가능하게. ✕는 항상 필수 유지. props로 헤더 영역 슬롯 제공하는 방식 검토.

## 배치/정렬

- [ ] **4. 스냅(자석) 정렬** — 카드끼리 가까워지면 경계가 딱 맞붙도록 스냅. 강하게 당기면 풀리고 다시 픽셀 단위 자유 이동. (필요성 보류 — 우선순위 낮음)
- [ ] **5. 그리드 정렬 기능** — 현재 띄운 카드들을 그리드 형태로 자동 재배치하는 명령/버튼.
- [ ] **6. 레이아웃 프리셋** — 카드 배치 상태를 여러 개 저장하고 빠르게 전환. #5와 연계해서 설계하면 좋을 듯.

## 앱 셸/메뉴

- [ ] **8. 앱 아이콘 추가** *(trivial)* — `src-tauri/icons/`의 기본 아이콘을 교체.
- [ ] **9. 타이틀바 색상 변경** *(trivial)* — 현재 OS 기본(흰색) 타이틀바. `tauri.conf.json`의 window 설정에서 다크 테마/커스텀 색상 적용.
- [ ] **10. "+ 플러그인" 버튼을 좌상단 햄버거 메뉴로 통합** — 플러그인 추가뿐 아니라 앱 전체 설정(향후 #6 프리셋 전환, #12 로그인, #13 테마 등)을 모으는 메뉴로 확장.
- [ ] **13. 폰트 변경** — 앱 전체 기본 폰트 교체.
- [ ] **14. 테마 설정** — 다크/라이트 등 테마를 #10 설정 메뉴에서 전환 가능하게. #9(타이틀바 색상), #13(폰트)와 함께 디자인 토큰화해서 관리하면 좋을 듯.

## 계정/마켓 (장기)

- [ ] **12. 로그인 + 사용자별 플러그인/레이아웃** — 사용자별로 설치된 플러그인 목록과 레이아웃이 다름. 로그인 시 해당 사용자의 플러그인 목록과 비교해 누락된 플러그인은 마켓에서 자동 다운로드. **선행 조건**: 플러그인 마켓(동적 로딩) 자체가 미구현 — CLAUDE.md의 "미구현/향후 과제" 항목과 강하게 연결됨. 백엔드(계정/마켓 서버) 설계 필요.

---

## 메모
- 2, 3은 모두 "youtube/teams 웹뷰가 항상 최상단" 제약과 얽혀 있어 함께 설계하는 게 효율적일 수 있음.
- 5, 6은 레이아웃 직렬화/복원 로직을 공유하므로 같이 가져가면 좋음.
- 12는 다른 항목들과 독립적이지 않음 — 마켓(동적 로딩) 인프라가 먼저 필요.
- 9, 13, 14는 전부 "디자인/테마" 영역이라 한 번에 토큰(색상/폰트 변수)으로 정리하면서 진행하면 효율적.

## 마켓형 플러그인 전환 과제

- [ ] **외부 플러그인 실행 모델 결정**: 메인 앱 컨텍스트에서 JS를 직접 `import()`할지, iframe/Tauri WebView로 격리 실행할지 결정한다. 생태계형 마켓을 목표로 하면 기본값은 격리 실행이어야 한다.
- [ ] **플러그인 manifest 표준화**: `id`, `name`, `version`, `entry`, `icon`, `defaultSize`, `minSize`, `permissions`, `apiVersion` 필드를 정의한다.
- [ ] **권한 모델 설계**: `network`, `storage`, `notification`, `openExternal`, `clipboard` 같은 권한을 manifest에 선언하고 설치 전에 사용자에게 표시한다.
- [ ] **플러그인 bridge API 설계**: 외부 플러그인이 앱 기능을 직접 만지지 못하게 하고 `lifedash.storage`, `lifedash.http`, `lifedash.notify`, `lifedash.ui` 같은 제한 API만 제공한다.
- [ ] **플러그인별 저장소 격리**: `pluginId`와 설치 인스턴스를 기준으로 namespace를 분리하고, 다른 플러그인 데이터 접근을 차단한다.
- [ ] **로컬 설치소 구현**: 마켓 연동 전 단계로 로컬 디렉터리 또는 zip 패키지를 설치하고 manifest를 읽어 대시보드에 표시하는 로더를 만든다.
- [ ] **마켓 API 계약 정의**: 플러그인 목록, 상세 정보, 다운로드 URL, 버전, 권한, 서명 정보를 내려주는 API 스키마를 만든다.
- [ ] **서명 및 무결성 검증**: 마켓에서 받은 플러그인 패키지가 변조되지 않았는지 확인한다. 외부 개발자 생태계에서는 선택 기능이 아니라 필수 안전장치다.
- [ ] **업데이트 정책 설계**: 자동 업데이트 여부, 권한 변경 시 재승인, 호환되지 않는 `apiVersion` 처리 방식을 정한다.
- [ ] **현재 내장 플러그인과의 공존 전략**: `src/plugins` 기반 내장 플러그인과 마켓에서 설치되는 외부 플러그인을 같은 Dashboard에서 어떻게 구분하고 렌더링할지 정한다.
